# Imapsync dependencies
- Bullseye requires package libcrypt-openssl-pkcs12-perl

# Docker requirements
- Update binfmt/QEMU in Docker Desktop
    `docker run --rm --privileged tonistiigi/binfmt --install all`
